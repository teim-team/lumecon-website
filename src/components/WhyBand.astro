---
/**
 * Why Lumecon — four cards on the white, hairline surface, each
 * headed by a piece of the supplied brand artwork (transparent
 * teal icon set, served from /art).
 */
import '../styles/home.css';

const CARDS = [
  {
    k: 'Every geography',
    t: 'County to nation',
    b: 'Counties, states, the nation, reservations and trust lands are included in every plan. No geography add-ons.',
    art: 'geo',
  },
  {
    k: 'Unlimited analysis',
    t: 'No per-analysis fees',
    b: 'Run unlimited projects and analyses on the full Lumecon model with one annual subscription.',
    art: 'rerun',
  },
  {
    k: 'Cedar included',
    t: 'An AI economic analyst',
    b: 'Cedar reads PDFs, spreadsheets and CSVs, structures the inputs the model needs, helps interpret results and supports reporting. It is included in every plan.',
    art: 'cedar',
  },
  {
    k: 'Every kind of organization',
    t: 'Built to adapt',
    b: 'One platform, with workflows built for governments, universities, nonprofits, businesses, Tribal Nations and consultants.',
    art: 'orgs',
  },
];
---

<section class="whyw" id="why" aria-labelledby="whyw-title">
  <div class="whyw-inner">
    <header class="whyw-head">
      <p class="h-kicker">Why Lumecon</p>
      <h2 class="whyw-title" id="whyw-title">Every plan gets the real platform.</h2>
      <p class="whyw-lede">
        Higher plans add collaboration, organizational memory and scale. The economics stay the
        same: every customer runs the same model on the same data foundation.{' '}
        <a class="inline-link" href="/pricing">Compare plans</a>{' '}or{' '}
        <a class="inline-link" href="/signup">start free</a>.
      </p>
    </header>
    <div class="whyw-grid">
      {
        CARDS.map((c) => (
          <article class="whyw-card">
            <span class="whyw-art" aria-hidden="true">
              <img
                src={`/art/why-${c.art}.png`}
                alt=""
                width="300"
                height="300"
                loading="lazy"
                decoding="async"
              />
            </span>
            <p class="whyw-card__k">{c.k}</p>
            <h3 class="whyw-card__t">{c.t}</h3>
            <p class="whyw-card__b">{c.b}</p>
          </article>
        ))
      }
    </div>
  </div>
</section>
