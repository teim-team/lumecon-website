---
/**
 * Product tour — alternating feature rows built from real
 * screenshots of the Lumecon workspace (sample analysis data), each
 * with a dark-mode capture of the app's own dark theme, plus a wide
 * region-map strip. The rows showcase screens the hero trio only
 * hints at: number lineage, comparison and the living workspace.
 */
import '../styles/tour.css';

const ROWS = [
  {
    id: 'trace',
    kicker: 'Defensible by design',
    title: 'Trace any number back to its layers',
    body: 'Every figure includes a “Trace this number” control. One click reveals the lineage: direct, indirect and induced effects, the operations that generate them and the industries where those effects land.',
    img: '/app/trace.webp',
    imgDark: '/app/trace-dark.webp',
    alt: 'Lumecon lineage panel titled Where $214,300,000 comes from, breaking economic output into direct, indirect and induced layers, by operation and by industry with share bars.',
  },
  {
    id: 'compare',
    kicker: 'Progress you can show',
    title: 'Compare analyses across years and scenarios',
    body: 'Put two analyses side by side and the change column does the arithmetic: jobs, labor income, GDP contribution, economic output and tax impacts, each with its percentage change. When the next budget cycle begins, the comparison is already there.',
    img: '/app/compare.webp',
    imgDark: '/app/compare-dark.webp',
    alt: 'Lumecon compare view of two community college expansion analyses, showing jobs, GDP contribution, labor income, economic output and tax impacts with green percentage-change values.',
  },
  {
    id: 'workspace',
    kicker: 'A living workspace',
    title: 'Analyses remain usable, from first draft to finished report',
    body: 'The workspace keeps every analysis in one place: drafts to continue, runs in progress and completed results ready to open, compare or export as a workbook, CSV tables or a printable summary.',
    img: '/app/dashboard.webp',
    imgDark: '/app/dashboard-dark.webp',
    alt: 'Lumecon workspace home with lanes for starting, in-progress and completed analyses, including completed analyses with jobs and output headlines.',
  },
];
---

<section class="tour" id="product" aria-labelledby="tour-title">
  <div class="tour-inner">
    <header class="tour-head">
      <p class="tour-kicker reveal-soft">Inside the product</p>
      <h2 class="tour-title reveal-soft" id="tour-title" data-stagger="1">
        Analysis that stands up to review.
      </h2>
      <p class="tour-lede reveal-soft" data-stagger="2">
        Built to be defensible in council chambers, boardrooms and grant reviews: model lineage
        behind every result, comparisons that do the arithmetic and a workspace built for recurring
        analysis.
      </p>
    </header>

    <div class="tour-rows">
      {
        ROWS.map((row, i) => (
          <article class={`tour-row${i % 2 === 1 ? ' tour-row--flip' : ''}`} id={`tour-${row.id}`}>
            <div class={`tour-row__text ${i % 2 === 1 ? 'reveal-right' : 'reveal-left'}`}>
              <p class="tour-row__kicker">{row.kicker}</p>
              <h3 class="tour-row__title">{row.title}</h3>
              <p class="tour-row__body">{row.body}</p>
            </div>
            <figure class={`tour-row__shot ${i % 2 === 1 ? 'reveal-left' : 'reveal-right'}`}>
              <picture>
                <source srcset={row.imgDark} media="(prefers-color-scheme: dark)" />
                <img
                  src={row.img}
                  alt={row.alt}
                  width="1600"
                  height="1000"
                  loading="lazy"
                  decoding="async"
                />
              </picture>
            </figure>
          </article>
        ))
      }
    </div>

    <figure class="tour-map reveal-soft">
      <picture>
        <source srcset="/app/map-card-dark.webp" media="(prefers-color-scheme: dark)" />
        <img
          src="/app/map-card.webp"
          alt="Lumecon region map of Connecticut with county boundaries, beside a panel reading Statewide, Connecticut: the statewide result."
          width="1600"
          height="458"
          loading="lazy"
          decoding="async"
        />
      </picture>
      <figcaption>
        Geographies follow the real economy: counties, states, the nation, reservations and trust
        lands are all first-class regions.
      </figcaption>
    </figure>
  </div>
</section>
